ff: True

model_dim: 128

preprocessor:
  input_dim: 6
  n_layers: 2
  n_heads: 4
  model_dim: ${model.model_dim}
  widening_factor: 4

encoder:
  n_layers: 6
  n_heads: 4
  model_dim: ${model.model_dim}
  widening_factor: 4

decoder:
  n_layers: 4
  n_heads: 4
  model_dim: ${model.model_dim}
  widening_factor: 4

compile: true
