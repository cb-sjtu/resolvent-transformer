ff: True

model_dim: 128
f_input_dim: 2
g_input_dim: 1
g_output_dim: 1
dropout: 0.0

encoder:
  n_layers: 6
  n_heads: 4
  model_dim: ${model.model_dim}
  widening_factor: 4
  dropout: ${model.dropout}

decoder:
  n_layers: 4
  n_heads: 4
  model_dim: ${model.model_dim}
  widening_factor: 4
  dropout: ${model.dropout}

compile: true
