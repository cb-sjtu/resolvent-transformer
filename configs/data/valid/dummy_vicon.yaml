dummy_vicon:
  name: dummy_vicon_valid
  description: "Vicon valid dummy data"
  dataset:
    _target_: src.datasets.dummy.DummyViconDataset
    demo_num: ${data.demo_num}
    cond_shape: ${data.cond_shape}
    qoi_shape: ${data.qoi_shape}

  num_workers: ${data.num_workers}
  pin_memory: ${data.pin_memory}
  batch_size_per_device: ${data.batch_size_per_device}
  random_across_devices: ${data.random_across_devices}
