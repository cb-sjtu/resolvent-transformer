defaults:
  - train:
    - eqn_snapshot_noslice
    - eqn_geometry
    - eqn_material
  - valid:
    - mesh_multi_single
    - mesh_multi_geometry
    - mesh_multi_snapshot
    - mesh_multi_geometry_train
    - mesh_multi_single_train


train_geometries: ['C', 'D', 'E', 'F', 'G', 'H']
valid_geometries: ['A', 'B']
train_materials: ['80percent', 2459]
valid_materials: ['20percent', 2459]

data_folder: ${paths.data_dir}/LargeMaterialModel-Elastic
batchsize: 16
rotate: "no"
flip: "no"
num_workers: 2
infinite: True
print_info: False
pin_memory: False