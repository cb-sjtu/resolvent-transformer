dummy_ol:
  name: dummy_ol
  description: "Operator Learning dummy data"

  dataset:
    _target_: src.datasets.dummy.DummyOperatorDataset
    f_seq_len: 100
    g_seq_len: 50
    f_inout_dim: 2
    g_in_dim: 1
    g_out_dim: 1

  batch_size_per_gpu: ${data.batch_size_per_gpu}

