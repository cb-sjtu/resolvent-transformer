dummy_ol_large:
  name: train_dummy_ol_large
  dataset:
    _target_: src.datasets.dummy.DummyOperatorDataset
    f_seq_len: 200
    g_seq_len: 100
    f_inout_dim: 2
    g_in_dim: 1
    g_out_dim: 1

  num_workers: ${data.num_workers}
  pin_memory: ${data.pin_memory}
  batch_size_per_device: ${data.batch_size_per_device}
  enable_device_seed: ${data.enable_device_seed}
