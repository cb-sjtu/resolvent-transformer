This folder contains the data configs.

`train` will be a list of data configs. In lightning datamodule, we will cycle through the these datasets.

`valid` will be a list of data configs. In lightning datamodule, we will create a list of dataloopers for separate validation.
