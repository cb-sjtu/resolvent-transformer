_target_: src.models.encoder_decoder.EncoderDecoder

encoder_in_proj:
  _target_: torch.nn.Linear
  f_input_dim: 2
  model_dim: 128

decoder_in_proj:
  _target_: torch.nn.Linear
  g_input_dim: 1
  model_dim: 128

encoder:
  _target_: src.models.transformer.get_transformer
  n_layers: 6
  n_heads: 4
  model_dim: 128
  widening_factor: 4
  dropout: 0.0

decoder:
  _target_: src.models.transformer.get_transformer
  n_layers: 6
  n_heads: 4
  model_dim: 128
  widening_factor: 4
  dropout: 0.0

out_proj:
  _target_: torch.nn.Linear
  model_dim: 1

compile: true
ff: true