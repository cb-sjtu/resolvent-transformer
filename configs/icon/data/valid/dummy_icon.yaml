dummy_icon:
  name: dummy_icon_valid
  description: "Icon valid dummy data"
  dataset:
    _target_: src.data.datasets.dummy_icon.IconDataset
    demo_num: ${data.demo_num}
    cond_shape: ${data.cond_shape}
    qoi_shape: ${data.qoi_shape}
    
  batch_size_per_process: ${data.batchsize}